<template>
  <div v-for="child in children.children">
    <h3>자식 페이지입니다.</h3>
    <p>이름 : {{ child.name }}</p>
    <p>나이 : {{ child.age }}</p>
    <p>용돈 : {{ child.balance }}</p>
    <button @click="buttonClick(child)">용돈 주세요.</button>
  </div>
</template>

<script setup>
const children = defineProps({
                    children: Array,
                  })

const emit = defineEmits(['giveMeAllowance'])

const buttonClick = function (child) {
  emit('giveMeAllowance', child)
}
</script>

<style scoped>

</style>